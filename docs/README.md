# docs/

**Runbooks** — worked, code-grounded guides for *editing* this repo. They lead with real skeletons (worked examples
beat prose for agent edit-success), stay scoped, and are plain-linked (never `@`-imported) so they load only when opened.

- [runbooks/add-a-funnel-step.md](./runbooks/add-a-funnel-step.md) — add an LLM tool + its card end-to-end.

The **authoritative** project docs stay at the repo root (not duplicated here): [`../AGENTS.md`](../AGENTS.md) (memory +
Engineering Constitution), [`../spec.md`](../spec.md) (the contract; §A is the current build), and
[`../CLAUDE.md`](../CLAUDE.md) (agent quick-start). Add a runbook only when there's a concrete, repeatable edit task —
not architecture prose.
